export interface ILoginInputs {
    email: string;
    password: string;
}

export interface IRegistrationInputs {
    name: string;
    email: string;
    password: string;
    confirmPassword: string;
}