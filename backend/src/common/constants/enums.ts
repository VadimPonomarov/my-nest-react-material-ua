export enum RoleEnum {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum JwtEnum {
  ACCESS = 'ACCESS',
  REFRESH = 'REFRESH',
  ACTIVATE = 'ACTIVATE',
}

export enum ResEnum {
  SUCCESS = 'Success 👍',
  FAILURE = 'Failure 👎',
}
